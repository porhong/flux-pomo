appId: com.porhong.fluxpomo
productName: Flux Pomo
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  - '!.github/*'
asarUnpack:
  - resources/**
win:
  executableName: FluxPomo
  # Relative to project; also copied into buildResources as icon.ico
  icon: build/icon.ico
  # Required for rcedit to embed icon + metadata into the .exe
  signAndEditExecutable: true
  # Portable .exe — run without installing
  target:
    - target: portable
      arch:
        - x64
portable:
  artifactName: ${name}-${version}-portable.${ext}
  requestExecutionLevel: user
  unpackDirName: flux-pomo-portable
mac:
  entitlementsInherit: build/entitlements.mac.plist
  notarize: false
  target:
    - target: zip
      arch:
        - universal
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
  icon: build/icon.png
  maintainer: porhong
  category: Utility
appImage:
  artifactName: ${name}-${version}-portable.${ext}
npmRebuild: false
publish:
  provider: github
  owner: porhong
  repo: flux-pomo
